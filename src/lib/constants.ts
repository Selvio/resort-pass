/**
 * Default radius in miles for location-based filtering
 */
export const DEFAULT_LOCATION_RADIUS_MILES = 50;

/**
 * Earth's radius in miles (used for Haversine distance calculation)
 */
export const EARTH_RADIUS_MILES = 3959;

/**
 * Minimum rating threshold for "top rated" hotels filter
 */
export const TOP_RATED_MIN_RATING = 4.5;
